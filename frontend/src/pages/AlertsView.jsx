import React from 'react';

const AlertsView = () => {
  return (
    <div className="kocc-card fade-in" style={{ textAlign: 'left' }}>
      <h2 className="card-section-title">Alertes Décrochage (IA MBENE)</h2>
      <p style={{ color: 'var(--text-secondary)', marginTop: '0.5rem' }}>
        Cette section présentera les recommandations pédagogiques pour les élèves à risque de décrochage.
      </p>
    </div>
  );
};

export default AlertsView;
