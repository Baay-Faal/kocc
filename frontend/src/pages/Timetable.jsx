import React from 'react';

const Timetable = () => {
  return (
    <div className="kocc-card fade-in" style={{ textAlign: 'left' }}>
      <h2 className="card-section-title">Emploi du Temps</h2>
      <p style={{ color: 'var(--text-secondary)', marginTop: '0.5rem' }}>
        Cette section affichera le calendrier des cours réels de votre classe.
      </p>
    </div>
  );
};

export default Timetable;
